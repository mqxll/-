double bisect(double (*f)(double), double a, double b, double eps=1e-6) {
    double c=(a+b)/2;
    if(b-a<eps) return c;
    if(f(a)*f(c)<=0) return bisect(f,a,c,eps);
    return bisect(f,c,b,eps);
}