bool cmpFiles(const std::string& f1,const std::string& f2) {
    std::ifstream a(f1), b(f2); char ch1,ch2;
    while(a.get(ch1) && b.get(ch2)) if(ch1!=ch2) return false;
    return a.eof() && b.eof();
}